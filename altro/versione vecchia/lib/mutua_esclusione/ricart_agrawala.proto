syntax = "proto3";

service RicartAgrawala {
  rpc RequestAccess (Request) returns (Response);
  rpc ReleaseAccess (Request) returns (Response);
}

message Request {
  string node_id = 1;
  int64 timestamp = 2;
}

message Response {
  bool granted = 1;
}
